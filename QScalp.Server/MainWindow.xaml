<Window x:Class="QScalp.Server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QScalp Server" Width="620" Height="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Closing="Window_Closing">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Settings -->
        <GroupBox Grid.Row="0" Header="Настройки подключения" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Content="API Base URL:"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="tbApiBaseUrl"/>

                <Label Grid.Row="1" Content="WS Base URL:"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="tbWsBaseUrl"/>

                <Label Grid.Row="2" Content="API Key:"/>
                <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="tbApiKey"/>

                <Label Grid.Row="3" Content="Listen URL:"/>
                <TextBox Grid.Row="3" Grid.Column="1" x:Name="tbListenUrl"/>

                <Label Grid.Row="4" Content="Max Clients:"/>
                <TextBox Grid.Row="4" Grid.Column="1" x:Name="tbMaxClients" Width="60" HorizontalAlignment="Left"/>

                <CheckBox Grid.Row="5" Grid.Column="1" x:Name="cbDebugMode" Content="Debug Mode" Margin="0,6"/>

                <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" Margin="0,6">
                    <Button x:Name="btnStart" Content="▶  Запустить" Width="120" Height="30"
                            Click="BtnStart_Click" FontWeight="SemiBold"/>
                    <Button x:Name="btnStop" Content="■  Остановить" Width="120" Height="30"
                            Margin="8,0,0,0" Click="BtnStop_Click" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Clients -->
        <GroupBox Grid.Row="1" Header="Подключённые клиенты" Padding="4" Margin="0,6,0,0">
            <ListView x:Name="lvClients" Height="100"
                      FontFamily="Consolas" FontSize="11">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Адрес" Width="150" DisplayMemberBinding="{Binding Address}"/>
                        <GridViewColumn Header="Тикер" Width="80" DisplayMemberBinding="{Binding Ticker}"/>
                        <GridViewColumn Header="Подключён" Width="100" DisplayMemberBinding="{Binding ConnectedAt}"/>
                        <GridViewColumn Header="Сообщений" Width="90" DisplayMemberBinding="{Binding MessageCount}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!-- Log -->
        <GroupBox Grid.Row="2" Header="Лог" Padding="4" Margin="0,6,0,0">
            <TextBox x:Name="tbLog" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                     TextWrapping="Wrap" FontFamily="Consolas" FontSize="11"
                     Background="#FF1E1E1E" Foreground="#FFDCDCDC"
                     AcceptsReturn="True"/>
        </GroupBox>
    </Grid>
</Window>
